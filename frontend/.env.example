# Frontend API URL (leave blank for dynamic LAN host detection)
VITE_API_URL=

# Frontend Environment Mode ('local' or 'cloud')
# In 'cloud', the UI reflects live cloud database connectivity without local sync controls
VITE_APP_ENV=local
